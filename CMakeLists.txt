CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(mpcap)
SET(CMAKE_BUILD_TYPE DEBUG)
SET(MPCAP_SHARED_LIBRARY mpcap_shared_library)
SET(MPCAP_STATIC_LIBRARY mpcap_static_library)
SET(MPCAP_EXECUTABLE mpcap_executable)

ADD_SUBDIRECTORY(src)
